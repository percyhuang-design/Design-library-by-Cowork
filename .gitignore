# private design-system source — clone separately, see BUILD.md
.source/

# dependencies
node_modules/

# platform build pipeline — transient copies & outputs (keep build/ SOURCE committed)
build/node_modules/
build/package.json
build/package-lock.json
build/App.jsx
build/previews.jsx
build/*.bundle.js
build/*.bundle.css
build/app.css
build/demos.css

# template build output
dist/
storybook-static/

# environment
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# macOS
.DS_Store
Icon
Icon?

# editor
.vscode/
.idea/
.claude/settings.local.json
.claude/logs/

# temp / caches
tmp/
.cache/
*.tsbuildinfo
.turbo/
.next/
.parcel-cache/
coverage/

# netlify CLI per-user state (site id / auth — per-user, not shareable)
.netlify/
